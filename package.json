{
  "name": "gqim-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint src tests scripts",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:export": "node scripts/verify-export.mjs",
    "verify": "npm run lint && npm test && npm run build && npm run verify:export"
  },
  "dependencies": {
    "next": "^16.2.10",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "eslint": "^9",
    "eslint-config-next": "^16.2.10",
    "vitest": "^3.2.7"
  }
}
